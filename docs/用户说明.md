# 用户说明 —— mediaplaner 点位管理系统

> 适用对象：销售、媒介调度、客管及运营人员
> 交付形态：单文件 `点位管理系统.html`（双击即可在浏览器打开，无需安装、无需联网）

---

## 1. 快速开始

1. 找到 `点位管理系统.html`，**双击**用 Chrome / Edge 等现代浏览器打开。
2. 顶部显示「点位管理系统 · 户外社区媒体 · 选点 → 锁点 → 发布 工作流」，并提示已内置的点位总数。
3. 无需登录、无需服务器。所有操作数据保存在**本机浏览器**中（刷新不丢）。

> 提示：若多人共用一台电脑或更换设备，数据不互通；如需团队协同，请等待后端版本。

---

## 2. 四大功能视图

顶部导航有四个标签页：**总览 / 点位管理 / 客户管理 / 投放计划**。

### 2.1 总览
- 一屏看清资源总量、计划进度、客户分布。
- 选中某计划或客户可快速跳转查看明细。

### 2.2 点位管理
这是日常使用最频繁的页面，用于**找资源、看状态、选点**。

**筛选与搜索**
- 按「媒体类型」（电梯框架 / 单元门智能框架）、「城市」「区域」「楼盘级别」「重复状态」「行业限制」组合筛选。
- 在搜索框输入楼盘名称或地址关键字，实时模糊匹配。

**状态含义（颜色徽标）**
| 状态 | 颜色 | 含义 |
| --- | --- | --- |
| 可选 | 灰 | 未被占用，可被选入计划 |
| 已选 | 蓝 | 已加入某计划，尚未锁位 |
| 已锁 | 橙 | 计划已锁位，期间不可被其它计划占用 |
| 已发布 | 绿 | 已完成上画发布 |

**批量选点**
1. 勾选需要的点位（可配合筛选缩小范围）。
2. 点击「加入计划」，选择目标投放计划（或新建）。
3. 点位状态变为「已选」，计划状态推进为「已选」。

**重复状态说明**
- **独占**：仅一家供应商覆盖。
- **跨来源重复**：≥2 家供应商均有该楼盘（需确认归属、避免重复投放）。
- **来源内重复**：同一供应商内部重名。

### 2.3 客户管理
- **新增客户**：填写客户名称、品牌、行业、城市、联系人、电话等，保存即建。
- **授权状态**：维护「未授权 / 审批中 / 已授权」。只有已授权客户才能进入正式投放。
- **客户分类**：标记 A / B / C（A=在投；B=潜力，受保护期限制；C=公海）。
- **编辑 / 删除**：选中客户后操作。

### 2.4 投放计划
投放计划是「选点 → 锁点 → 发布」的载体。

**新建计划**
- 填写计划名称、关联客户、媒体类型、城市、发布周期等，保存后为「草稿」。

**选点**
- 进入计划 → 选点 → 跳到点位管理勾选点位加入，或在本页直接选。
- 选点完成后计划状态：草稿 → 选点中 → 已选。

**锁点**
- 计划选点就绪后点击「锁点」。
- 计划内全部点位置「已锁」，计划状态 →「已锁」。
- 锁位期间其它计划不可占用这些点位。

**发布 / 上画**
- 仅「已锁」计划可发布。点击「发布」。
- 系统生成**上画通知书**（发布明细 / 客户确认单），点位置「已发布」，计划状态 →「已发布」。

**删除计划**
- 删除后，其占用的点位自动回到「可选」，释放资源。

---

## 3. 典型工作流示例

> 场景：为「岚图汽车」在广州做一期双周电梯框架投放。

1. **建客户**：客户管理 → 新增「岚图汽车」，授权状态置「已授权」。
2. **建计划**：投放计划 → 新建「岚图 8 月广州双周」，关联客户、媒体类型=电梯框架、城市=广州。
3. **选点**：点位管理 → 筛选「广州 + 电梯框架 + 楼盘级别 A+」→ 勾选 80 个点位 → 加入该计划。计划变「已选」。
4. **锁点**：进入计划 → 锁点。80 个点位变「已锁」。
5. **发布**：进入计划 → 发布 → 系统生成上画通知书，点位变「已发布」。
6. **复盘**：总览查看本期发布达成；如需释放，删除计划即可复位。

---

## 4. 数据说明与隐私

- 系统内置 **13,456** 条点位（电梯框架约 5,280 + 单元门智能框架约 8,176），来自配套的 SQLite 资源库。
- 所有业务数据（选点 / 锁点 / 客户 / 计划）仅存于**本机浏览器 localStorage**，不上传任何服务器。
- 资源库含真实楼盘地址与供应商信息，请注意保管，不要将文件随意分发。

---

## 5. 常见问题

**Q：打开是空白页 / 点按钮没反应？**
A：请使用 Chrome / Edge 等现代浏览器；不要用老旧 IE。若仍异常，尝试关闭浏览器硬件加速或换一台电脑测试。不要用微信 / QQ 内置浏览器打开。

**Q：数据会丢吗？**
A：正常刷新、关闭浏览器都不丢。但**清理浏览器缓存 / 换浏览器 / 换电脑**会丢失。重要计划建议截图或导出留存。

**Q：筛选结果和 Excel 对不上？**
A：以配套 SQLite 资源库（`data/*.db`）为主数据源；前端 `points.json` 由其导出，理论上一致。如怀疑差异，用 `data/manage.sql` 中的查询核对。

**Q：能改点位基础信息吗？**
A：点位静态属性（城市、楼盘、媒体数等）来自资源库，当前版本在系统内只读；可通过修改 db + 重新导出 json 更新（见调试文档）。

**Q：多人能协同吗？**
A：当前为单机版，无账号权限。协同与权限为规划中的 M2 / M3 版本。
