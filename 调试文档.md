# 调试文档 —— mediaplaner 点位管理系统

> 适用对象：开发人员 / 运维
> 目的：记录构建链路、已知坑、验证手段与排错流程，避免重复踩坑。

---

## 1. 工程结构

```
点位管理系统/
├── index.html              # Vite 入口
├── package.json
├── vite.config.js          # 单文件构建配置
├── _pp.cjs                 # 构建后处理（去 module 标记）
├── _jsdom_test.cjs         # 无头真跑验证（30 断言）
├── src/
│   ├── main.jsx            # 入口（含 DOMContentLoaded 守卫）
│   ├── App.jsx             # 根组件 + 状态机
│   ├── store.js            # 数据层 / localStorage 封装
│   ├── index.css           # Tailwind 样式
│   ├── components/         # Dashboard / PointManage / CustomerManage / PlanManage 等
│   └── data/points.json    # 前端内置点位（由 db 导出，约 6.4MB）
└── dist/                   # 构建输出
```

配套数据处理脚本（项目根目录）：
- `_build_db.py`：原始 Excel → SQLite `.db` + 资源总库 xlsx。
- `_build_unitdoor_db.py`：单元门库构建。
- `_export_json.py`：db → `points.json`。

---

## 2. 构建环境

| 依赖 | 版本 / 路径 |
| --- | --- |
| Node | 托管版 22.22.2（`C:/Users/wolf2/.workbuddy/binaries/node/versions/22.22.2`） |
| Python | 托管版 3.13.12 + venv（`.../python/envs/default`，含 openpyxl / python-docx / pdfplumber） |
| npm | 须将托管 Node 加入 PATH，否则 `vite` 命令静默失败 |

**关键**：构建前务必
```bash
export PATH="C:/Users/wolf2/.workbuddy/binaries/node/versions/22.22.2:$PATH"
cd 点位管理系统
npm install
```

---

## 3. 已知坑与修复（务必记牢）

### 3.1 单文件构建后双击白屏（致命）
- **现象**：`vite build` + singlefile 把 `<script>` 提升到 `<head>`；若脚本是经典脚本（去掉 `type="module"` 后），会在 `#root` 渲染前执行 → `createRoot: Target container is not a DOM element`（React #299）。
- **根因**：singlefile 提升脚本位置 + 经典脚本无 defer。
- **修复**：`src/main.jsx` 加 `DOMContentLoaded` 守卫（见 3.2）。**不要**用移动 / 截取脚本的笨办法。
- **后处理只去标记**：`_pp.cjs` 仅把 `<script type="module">` 替换为 `<script>`，**不截取、不移动**脚本内容（正则截取会把包体内的 `</script>` 截断导致 `Unexpected token '<'`）。

### 3.2 main.jsx 守卫（标准形式）
```jsx
function mount() {
  const el = document.getElementById('root');
  if (!el) return;
  createRoot(el).render(<React.StrictMode><App /></React.StrictMode>);
}
if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', mount);
} else { mount(); }
```

### 3.3 npm 装包失败 `[safe-delete] 操作失败`
- **现象**：Windows 回收站拦截 npm 缓存写入。
- **修复**：指定独立缓存目录并绕过沙箱：
  ```bash
  npm install --cache D:/tmp_npmcache
  ```
  （必要时 `dangerouslyDisableSandbox: true`）

### 3.4 vite build 静默不更新 dist
- 多为 PATH 未含托管 Node。先 `export PATH=...` 再用 `./node_modules/.bin/vite build`。

### 3.5 jsdom 测试 `Assignment to constant variable`
- 测试脚本中 `ov` 用 `const` 又在锁点 / 发布后重读赋值。改为 `let`。

---

## 4. 验证手段（发布前必做）

### 4.1 jsdom 无头真跑（核心）
`_jsdom_test.cjs` 用 jsdom 真实加载 `点位管理系统.html` 并模拟全流程，断言关键点位状态流转。**语法检查 ≠ 运行正确**，必须真跑。

运行：
```bash
export PATH="C:/Users/wolf2/.workbuddy/binaries/node/versions/22.22.2:$PATH"
NODE_PATH="C:/Users/wolf2/.workbuddy/binaries/node/workspace/node_modules" \
  node _jsdom_test.cjs
```
要点：
- jsdom 需 `url:'http://localhost/'` 让 localStorage 可用。
- 需托管 Node 的 `NODE_PATH`（Windows 风格路径，Git Bash 的 `/c/...` 会 `MODULE_NOT_FOUND`）。
- 监听 `jsdomError`，否则 JS 崩溃是静默的。
- 必测场景：**全新打开（空 localStorage）**——开发时浏览器已有数据，永远命中不了这条分支，却最易崩。
- 当前版本 **30 项断言全部 PASS**。

### 4.2 数据层 SQL 验证
用 `data/manage.sql` 中的查询核对点位总数、重复分布、按城市统计，确认与前端一致：
```bash
python -c "import sqlite3; c=sqlite3.connect('data/岚图汽车_资源数据库.db'); print(c.execute('SELECT COUNT(*) FROM t_resource').fetchone())"
```

### 4.3 人工冒烟
按《用户说明》第 3 节走一遍：建客户 → 建计划 → 选点 → 锁点 → 发布 → 删除复位，确认状态机正确且刷新保留。

---

## 5. 数据重建流程

1. 更新原始 Excel（4 份供应商表）。
2. 跑 `_build_db.py` / `_build_unitdoor_db.py` 重建 `.db` 与资源总库 xlsx。
3. 跑 `_export_json.py` 导出 `src/data/points.json`。
4. `npm run build` → `_pp.cjs` 后处理 → 得到新 `点位管理系统.html`。
5. 跑 `_jsdom_test.cjs` 回归。

---

## 6. 常见报错速查

| 报错 / 现象 | 可能原因 | 处理 |
| --- | --- | --- |
| `Target container is not a DOM element` | head 脚本抢跑 | 加 DOMContentLoaded 守卫（3.2） |
| `Unexpected token '<'` | 后处理正则截断脚本 | 仅替换开标签，不截取（3.1） |
| dist 不更新 | PATH 缺托管 Node | export PATH 后重 build（3.4） |
| npm `[safe-delete] 失败` | 回收站拦截缓存 | `--cache D:/tmp_npmcache`（3.3） |
| jsdom `MODULE_NOT_FOUND` | NODE_PATH 用 `/c/` | 改用 Windows 风格路径（4.1） |
| 打开空白 / 按钮无反应 | 用了 IE / 内置浏览器 | 换 Chrome / Edge（见用户说明 FAQ） |
| 刷新丢数据 | 清缓存 / 换浏览器 | 重要计划截图留存（用户说明 4） |
