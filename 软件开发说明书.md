# 软件开发说明书 —— mediaplaner 户外社区媒体点位管理系统

> 版本：V1.0
> 编写依据：内部《OA 管理系统使用指南》《报价申请及选位申请指南》《销售部对接流程》《销售工作流程》等流程文档。
> **脱敏声明**：本文档已去除原资料中的公司名称与所有个人姓名，角色统一抽象为岗位职责（销售、媒介、客管、城市主管、运营、财务、审批领导等）；原系统访问地址、账号等敏感信息一律省略。

---

## 1. 文档目的

本文档定义 mediaplaner 的系统架构、业务流程、功能模块、数据模型与部署方式，作为开发、测试、运维与后续迭代的统一依据。系统面向**户外社区媒体**（社区电梯框架广告、单元门智能框架广告）的投放管理，固化「选点 → 锁点 → 发布」标准作业流程。

---

## 2. 系统概述

### 2.1 业务背景
户外社区媒体资源分散于多家供应商、多个城市。业务上需要先做**客户授权**，再走**报价 → 选位 → 锁位 → 合同 → 转销售 → 上画通知 → 监测 → 发票 → 收款**的完整链路。系统用统一的状态机把"资源 — 客户 — 计划"三要素串联。

### 2.2 技术架构
| 层 | 技术 | 说明 |
| --- | --- | --- |
| 表现层 | React 18 + Tailwind CSS | 单页应用，4 个主视图 |
| 构建 | Vite + vite-plugin-singlefile | 打包为单文件 HTML，双击 `file://` 运行 |
| 数据层（主数据） | SQLite（`*.db`） | 资源库权威数据源 |
| 数据层（运行态） | `points.json`（构建内置）+ `localStorage` 覆盖层 | 状态变更不复制整表 |
| 数据处理 | Python（openpyxl / sqlite3） | 原始 Excel → 清洗 → db / json |

### 2.3 模块划分
1. **总览（Dashboard）**：资源、客户、计划三要素的汇总看板。
2. **点位管理（PointManage）**：资源检索、筛选、状态可视化、批量选点。
3. **客户管理（CustomerManage）**：客户 CRUD、授权状态、A/B/C 分类。
4. **投放计划（PlanManage）**：计划 CRUD + 状态机（选点 / 锁点 / 发布）。

---

## 3. 角色与职责（脱敏）

| 角色 | 职责 |
| --- | --- |
| 销售 / 客户经理 | 新建客户、发起授权与选位、推进计划、提交上画通知 |
| 媒介调度 | 按选位需求匹配资源、关联方案、核对转销售明细 |
| 客户管理（客管） | 客户授权审批、A/B/C 分类维护、冲突裁决 |
| 城市主管 / 城市总监 | 辖区内报价与选位初审 |
| 运营部 | 报价复核、流程规范 |
| 财务 | 合同额 / 回款 / 发票审核 |
| 审批领导 | 合同、上画、特殊事项的最终审批 |
| 销售助理 | 合同登记、监测报告送达归档 |

> 说明：当前版本以状态字段体现流程节点，未实现独立审批引擎；角色权限统一在后续版本以账号体系落地。

---

## 4. 核心业务流程

### 4.1 客户管理流程
1. **新建客户**：录入客户全称（与营业执照一致）、简称、品牌、行业、城市、联系人、电话等。代理公司签约需另填广告公司信息。
2. **授权申请**：客户录入后即可发起授权。授权审批先经本城市销售负责人，最终由客管确认。授权后客户状态变为"已授权"，方可签署合同。
3. **拜访记录**：未授权前可多次拜访并留存记录，作为后续客户冲突裁决依据之一。
4. **客户分类**：
   - **A 类**：已签约正在发布中（数量不限；发布结束超半年未续约自动降 C）。
   - **B 类**：跟进中潜力客户（每销售上限 12 个，保护期半年；签约自动升 A）。
   - **C 类**：公海客户，申请需附联系人名片。
5. **授权数量**：每销售授权配额 = 过去一年有执行发布的客户数 N + 15。

### 4.2 报价申请流程
- **适用范围**：所有客户（含未授权客户）均可发起报价；未授权客户报价后客户信息不可再改。
- **一次性合同**：无外购时可直接选位，但合同申请前应先报价；同客户同城市二次发布可自动带出已审批报价，免重复申请。
- **年度合同**：先申请合同，每次发布前先报价再选位；无外购时也须在申请发布确认书前先报价。
- **审批链**：城市主管 →（含外购时）外购核算 → 运营部 →（超权限）全国总监。

### 4.3 选位申请流程
- **前提**：客户须已授权（未授权客户选位后客户信息锁定不可改）。
- **一次性合同**：先选位 → 再合同。
- **年度合同**：先合同 → 在合同基础上选位（所选城市/周期须与年度合同一致）。
- **选位要求要素**（详见附录 A）：
  - 一梯一位 / 一洞一位 / 其他；
  - 客货消是否有要求；护板是否有要求；
  - 等候厅 / 大堂是否选；
  - 是否限制写字楼 / 商住楼 / 住宅楼；
  - 楼盘级别（A++ / A+ 等）；售价与租价限制；
  - 每楼盘最大选位数；多周期滚动发布规则；
  - 指定楼盘 / 指定门店周边公里数；其他特殊要求。
- **选位原则**：发布周期遵循"周六开始、周五结束"。

### 4.4 锁位申请流程（非必须）
- 选位经媒介关联方案后，方可发起锁位。
- **锁位时长规则**：
  - 区域客户：单次可锁 3 个工作日，连续最多 3 次（合计 9 个工作日）。
  - 4A 客户：单次可锁 7 个工作日，连续最多 2 次（合计 14 个工作日）。
- **总量约束**：单城市锁位 + 转销售方案数量超过当地签约资源 60% 时，后续锁位须有已审批合同或经相关负责人确认。
- **放位处罚**（锁位后取消占用）：
  - 按"离发布天数 × 连续锁位天数 × 锁位数量占比"判定处罚区间；
  - 上画率 ≥ 50% / ≥ 75% 对应更高单位罚款；
  - 处罚金额 = 取消版位数 × 单位罚款（上限 2000 元，旺季不设上限）。

### 4.5 合同申请流程
- **一次性合同**：选位通过后申请，填写成交价 / 合同额 / 付款方式（系统自动算折扣）。多人合作可分配比例。
- **年度合同**：先申请合同，再逐次选位与申请发布确认书。
- **审批链**：客管 → 城市负责人 → 财务 →（非标准条款）法务 → 审批领导 → 客管给合同号。
- **无合同上画**：发布前未取得双方盖章合同的，须以客户盖章的发布确认书（或官方邮件确认 / 刊前全款凭证）作为附件。

### 4.6 转销售流程（非必须）
合同签约前后，销售将客户确认的方案在系统提交转销售申请，附客户确认方案单；媒介核对与资源库一致后修改方案状态。

### 4.7 上画通知书（发布）流程
- 仅审批通过的合同可发起上画通知。
- 本地发布提前 3 个工作日、外地提前 5 个工作日提交《上画通知书》与《印刷申请单》。
- 年度合同每次上画前须先发起"发布确认书"申请。
- 发布周期默认"周六开始、周五结束"；非正常周期发布收取特殊上画费。

### 4.8 监测与查楼
- **监测拍照**：上画前至少 2 个工作日通知拍照要求；媒介换画后 3 个工作日内提供监测报告（完工报告 + 监测照片 + 调位通知书）。
- **刊后查楼**：发布期结束前可申请工程部陪查；超量发布可由公司提供免费租车查楼服务（置换 / 空位销售不享受）。

### 4.9 发票与收款
- 发票申请经客管审核；开票总额超出合同总额须说明。
- **账期**：区域直客刊前付清 / 结束后 3 月内；本土广告公司 1 月内；4A 及大客户 6 月内。
- 客管按月汇总欠款并协同财务催款。

### 4.10 空位销售规则（特殊）
- 空位销售**不享受锁位保护**，与正常销售冲突时无条件让位。
- 选位时间为每发布档期前 2 日，且仅做单周方案选位。
- 不享受免费租车查楼与第三方监测增值服务。
- 须在《选位申请表》《上画通知书》中明确标注为空位销售。

---

## 5. 功能模块详细设计

### 5.1 总览（Dashboard）
- 资源卡：电梯框架 / 单元门智能框架 点位总数与城市分布。
- 计划卡：各状态（草稿 / 选点中 / 已选 / 已锁 / 已发布）计划数。
- 客户卡：客户数与 A/B/C 分布。

### 5.2 点位管理（PointManage）
- 筛选维度：媒体类型、城市、区域、楼盘级别、重复状态、行业限制。
- 搜索：楼盘名称 / 地址关键字。
- 状态徽标：可选（灰）/ 已选（蓝）/ 已锁（橙）/ 已发布（绿）。
- 批量选点：勾选后加入指定计划。

### 5.3 客户管理（CustomerManage）
- CRUD：名称、品牌、行业、城市、联系人、电话。
- 授权状态：未授权 / 审批中 / 已授权。
- 分类：A / B / C。

### 5.4 投放计划（PlanManage）
状态机驱动：
```
草稿 → 选点中 → 已选 → 已锁 → 已发布
```
- 选点：加入点位，计划置"已选"。
- 锁点：计划内点位置"已锁"，计划置"已锁"。
- 发布：生成上画通知书，点位置"已发布"，计划置"已发布"。
- 删除计划：占用点位自动回到"可选"。

---

## 6. 数据模型

系统中存在两层数据：

### 6.1 主数据层（SQLite，权威）
两份资源库，均由原始供应商 Excel 经清洗脚本生成，结构见 `data/schema.sql`。

**电梯框架库表 `t_resource`（北上广深）** 关键字段：
`id, supplier, contact, phone, dup_status, dup_group, city, area, block, project, address, category, level, price, built, households, population, occupancy, floors, buildings, units, elevators, in_elev_media, hall_media, total_media, ad_size, restriction, audience, note, src_file, src_sheet, src_no`

**单元门智能框架库表 `t_resource`（全国 27 城）** 关键字段：
`id, supplier, contact, phone, dup_status, dup_group, province, city, area, project, address, community_type, buildings, unit_doors, price, households, movein_year, total_media, src_sheet, note`

两库均含 `t_supplier`（供应商对照：supplier, src_file, contact, phone, remark）。

### 6.2 运行态层（浏览器）
- `points.json`：由 db 导出、构建时内置（13,456 条）。
- `localStorage` 覆盖层（键 `pdgl_point_overrides_v1` / `pdgl_customers_v1` / `pdgl_plans_v1`）：仅保存状态变更与客户 / 计划，刷新不丢。

### 6.3 数据流向
```
原始 Excel ──清洗脚本(_build_db.py)──▶ SQLite .db（主数据）
                                         │
                                         └─导出──▶ points.json ──构建──▶ 单文件 HTML
                                                                           │
                                                  运行时 localStorage 覆盖层（状态变更）
```

---

## 7. 关键业务规则（算法摘要）

| 规则 | 取值 |
| --- | --- |
| 发布周期基准 | 周六上画、周五结束 |
| 区域客户锁位 | 3 工作日 × 最多 3 次 = 9 工作日 |
| 4A 客户锁位 | 7 工作日 × 最多 2 次 = 14 工作日 |
| 锁位总量阈值 | 单城市 > 签约资源 60% 须审批 |
| 放位处罚上限 | 单次 ≤ 2000 元（旺季不设上限） |
| 本地 / 外地前置 | 上画通知提前 3 / 5 个工作日 |
| 账期（直客 / 本土 / 4A） | 刊前 / 3 月 / 1 月 / 6 月 |

---

## 8. 部署与运行

- **交付物**：单文件 `点位管理系统.html`（双击即可运行，无需服务器与联网）。
- **数据**：配套 `data/` 下两份 `.db` 与 `points.json`、`schema.sql`、`manage.sql`。
- **二次开发**：`src/` 提供 React 源码，`npm install && npm run build` 可重新构建（见调试文档）。
- **数据安全**：资源库含真实楼盘与供应商信息，仓库访问须受控，不建议对外公开原始数据。

---

## 附录 A：选位要求要素表（脱敏整理）

| 序号 | 要素 | 说明 |
| --- | --- | --- |
| 1 | 一梯一位 / 一洞一位 / 其他 | 基础投放单元 |
| 2 | 客货消要求 | 是否限制客梯 / 货梯 / 消防梯 |
| 3 | 护板要求 | 是否要求带护板 |
| 4 | 等候厅 / 大堂 | 是否选等候厅或大堂位 |
| 5 | 楼盘性质 | 写字楼 / 商住楼 / 住宅楼限制 |
| 6 | 楼盘级别 | A++ / A+ 等 |
| 7 | 售价 / 租价 | 是否限制价格区间 |
| 8 | 单盘最大选位数 | 每楼盘上限 |
| 9 | 滚动发布 | 相邻滚动 / 全周期滚动；排梯 / 排洞 / 排楼 |
| 10 | 指定楼盘 | 提供楼盘表 |
| 11 | 门店周边 | 门店名称 + 地址 + 周边公里数 |
| 12 | 其他要求 | 文字补充说明 |

## 附录 B：广告画面尺寸规格（脱敏整理）

| 类型 | 成品尺寸 | 可视尺寸 |
| --- | --- | --- |
| 小框 | 424 × 570 mm | 390 × 540 mm |
| 大框（一线城市） | 575 × 770 mm | 530 × 730 mm |
| 大框（二线城市） | 590 × 790 mm | 534 × 734 mm |
| 智能屏（竖屏） | 268 × 476 mm（视屏） | 9:16，1080×1920 |

> 画面精度 300dpi、CMYK 模式、JPG 首选；须符合《广告法》及行业管理规定（医疗 / 房地产 / 金融 / 教育等）。
